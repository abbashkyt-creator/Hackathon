(function () {
  "use strict";
  // The original loader enters level one immediately after its local assets
  // decode. No click-through splash or portal play button is inserted.
  window.loadLang("en");
}());
